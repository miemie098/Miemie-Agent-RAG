# ============================================================
# Miemie-Agent-RAG 核心依赖
# ============================================================
# 安装: pip install -r requirements.txt
#
# 仅列出直接依赖，传递依赖由 pip 自动解析。
# 版本使用兼容范围 (>=)，避免锁死补丁版本。
# ============================================================

# --- Web 框架 ---
fastapi>=0.100.0
uvicorn[standard]>=0.30.0
python-dotenv>=1.0.0

# --- Agent 编排 ---
langgraph>=1.0.0
langchain-openai>=0.3.0
langchain-huggingface>=0.1.0

# --- 向量数据库 ---
pymilvus>=3.0.0
milvus-lite>=3.0.0

# --- 检索算法 ---
rank-bm25>=0.2.0

# --- Embedding & 精排模型 ---
sentence-transformers>=3.0.0
transformers>=4.40.0

# --- 推理加速 ---
onnxruntime>=1.20.0

# --- 深度学习 ---
torch>=2.0.0

# --- 文档解析 ---
pypdf>=5.0.0

# --- LLM SDK ---
openai>=2.0.0

# --- 模型下载 (国内镜像，仅 download_models.py 使用) ---
modelscope>=1.20.0
